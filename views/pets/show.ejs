<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home for Pets</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"/>
  </head>
  <body>
    <div class="container mt-4">
      <h4 class="mb-4"><i class="fa fa-arrow-left" onclick="location.href='/pets'"></i> Pet Details</h4>
      <div class="card">
        <div class="card-body">
          <p class="card-text"><strong>Name: </strong><%= pet.name %></p>
          <p class="card-text"><strong>Species: </strong><%= pet.species %></p>
          <p class="card-text"><strong>Gender: </strong><%= pet.gender %></p>
          <p class="card-text"><strong>Description: </strong><%= pet.description %></p>
          <p class="card-text"><strong>Location: </strong><%= pet.location %></p>
          <p class="card-text"><strong>photo: </strong><%= pet.photo %></p>
          <p class="card-text"><strong>Status: </strong><%= pet.status %></p>
        </div>
      </div>

      <% if (pet.owner) { %>
      <h4 class="mt-4 mb-4">Owner Contact Information</h4>
      <div class="card">
        <div class="card-body">
          <p class="card-text"><strong>Owner:</strong><%= ownerInfo.name %></p>
          <p class="card-text"><strong>Phone:</strong><%= ownerInfo.phone %></p>
          <p class="card-text"><strong>Email:</strong> <%= ownerInfo.email %></p>
        </div>
      </div>
      <% } %>

      <div class="mt-4 mb-4">
        <% if (user.id == pet.owner) { %>
        <a href="/pets/<%= pet._id %>/edit" class="btn btn-primary">Edit</a>
        <form action="/pets/<%=pet._id%>?_method=DELETE" method="POST" class="d-inline-block">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
        <% } %> 
        
        <% if (user.userType==='admin') { %> 
          <% if (!pet.owner) { %>
            <a href="/pets/<%= pet._id %>/edit" class="btn btn-primary">Edit</a>
          <% } %>
          <form action="/pets/<%=pet._id%>?_method=DELETE" method="POST" class="d-inline-block">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        <% } %>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
