<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home for Pets</title>
  </head>
  <body>
    <h1>All Pets</h1>
    <ul>
      <% pets.forEach(pet => { %>
      <li>
        <p>Name: <%= pet.name %></p>
        <p>Gender: <%= pet.gender %></p>
        <button onclick="location.href='/pets/<%= pet._id %>'">Details</button>
      </li>
      <br />
      <%}); %>
    </ul>
  </body>
</html>
